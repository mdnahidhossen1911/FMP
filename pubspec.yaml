name: fmp
executables:
  fmp: fmp

description: Auto-generated Flutter MVVM project with folder structure and boilerplate files.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.4.1

# Add regular dependencies here.
dependencies:
  args: ^2.4.2

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
